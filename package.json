{
  "name": "notddns",
  "version": "1.0.0",
  "description": "Simple Cloudflare Worker that records the requester's public IP in Workers KV as a DDNS alternative.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.23.0"
  }
}
